---
name: devspace-plugin-example
description: Example DevSpace plugin Skill used to verify plugin-provided Skill roots.
---

# DevSpace Plugin Example

Use the `plugin_devspace_example_echo` tool when the user explicitly asks to verify the example plugin execution path.

Pass a short `message` parameter and report the exact process result.
